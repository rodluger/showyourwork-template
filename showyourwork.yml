# Tell showyourwork about new script extensions and how to run them
scripts:
  # Tell showyourwork how to execute `.gv` files to produce figures.
  # The {script} and {output} placeholders resolve to the script file
  # name and figure file name, respectively, at run time.
  gv:
    dot -Tpdf {script} > {output}